# Phase 0 exit (2026-09-19)
- [ ] USB HID enumerates on PC, inputs verified
- [ ] USB XInput enumerates, inputs verified
- [ ] BLE pairs with PC + Android, Xbox-layout inputs verified
- [ ] Reconnect after power cycle works (NVS bond)
- [ ] RP2040 Pico build still green
- [x] `make -C tests/host test` green
- [ ] Parked (not built): dual-transport, USB-ECM webconfig, classic-ESP32 companion, NVS bonded-device UI

## Carry-forward to first S3/IDF environment
1. R1 closure — CI Pico-board build on branch `feature/esp32s3-poc`.
2. First `idf.py build` must work through Task-7's 10 CMake TBDs + Task-8's ~60 NimBLE TBDs + REQUIRES selection + `CFG_TUSB_MCU`/drivermanager trim.
3. Hardware validations (USB enumerate/inputs, BLE pair/inputs/reconnect).
4. Deferred minors index (pointer: see SDD ledger `progress.md` Tasks 1,2,3,4,5,6,8 entries).
